# 平台使用费收费标准配置 - 执行计划

> 创建日期：2026-06-04
> 状态：待执行

---

## 执行目标

更新 `设计文档/平台使用费收费标准配置_20260604.md`，增加以下内容：

1. 新增"租户个性化配置"章节（含代理项目收费开关）
2. 新增"示例场景：集团A"（项目类别 × 组织形式 2×2 矩阵）
3. 更新功能概述流程图
4. 补充业务规则（代理项目计费逻辑、数据表）

---

## 变更详情

### 变更1：功能概述流程图（第13-19行）

将单链路流程图扩展为双入口：

```
租户管理 → 平台使用费（配置招标类+非招类阶梯规则）
         → 个性化配置（代理项目收费开关，待扩展更多开关）
                    │
                    │ 写入租户收费规则 + 个性化配置
                    ▼
采购立项 → 标段包（读取收费规则，根据组织形式判断是否计费）
```

### 变更2：新增"租户个性化配置"章节（插入在原"四、业务规则"之前）

**章节编号调整**：
- 原"四、业务规则"→ 调整为"五"
- 原"五、与其他功能的关系"→ 调整为"六"
- 原"六、待确认问题"→ 调整为"七"

**新增内容**：

---

## 四、租户个性化配置

### 4.1 操作入口

**功能路径**：`租户管理 → 列表操作列 → 个性化配置`
**操作角色**：超级管理员

### 4.2 个性化配置弹窗

```
┌─ 租户个性化配置 ────────────────────────────────────────────┐
│                                                              │
│  * 代理项目是否收取平台使用费   [ 开 ○ / 关 ● ]（默认关）   │
│    说明：关闭后，委托采购（代理）项目不收取平台使用费        │
│                                                              │
│  ─── 更多配置待补充 ───                                      │
│                                                              │
│                [ 取消 ]  [ 确认 ]                            │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 数据结构（tenant_personalized_config）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 租户ID | 外键 | ✅系统自动 | 所属租户 |
| 代理项目收取平台使用费 | 布尔 | ✅ | 开/关，默认关 |

**说明**：
- 每个租户一条记录，首次打开个性化配置时自动创建
- 预留扩展能力，后续按需增加更多开关字段

---

### 变更3：新增"示例场景：集团A"

插入在个性化配置弹窗说明之后，作为 4.4 节：

---

## 4.4 示例场景：集团A

**计费维度**：项目类别（招标/非招）× 组织形式（自行采购/委托采购）= 4种组合

#### 收费规则配置

- **招标类规则**：\ [0, 无上限) → 0 元（实质免费）
- **非招类规则**：\ (0, 5]→0；(5, 20]→800元；(20, +∞)→1,200元
- **个性化配置**：代理项目收取平台使用费 = **关**

#### 计费矩阵

| 项目类别 | 组织形式 | 平台使用费 | 说明 |
|---------|---------|-----------|------|
| 招标 | 自行采购 | 0 元 | 按招标规则，全档位0元 |
| 非招 | 自行采购 | 按阶梯收费 | 按非招规则：(0,5]→0；(5,20]→800；(20,∞)→1200 |
| 招标 | 委托采购（代理） | 0 元 | 开关关闭，不收平台使用费 |
| 非招 | 委托采购（代理） | 0 元 | 开关关闭，不收平台使用费 |

**补充说明**：
- 委托采购（代理）项目的代理服务费在标段信息中配置（当前需求文档中尚未涉及委托场景，待后续设计）
- 如果开关开启，委托采购将与自行采购共用同一套招标/非招阶梯规则

---

### 变更4：业务规则增强（原"四"调整为"五"）

在业务规则表中新增 3 条：

| 编号 | 规则 | 说明 |
|------|------|------|
| R11 | 代理项目收费逻辑 | 个性化配置中"代理项目收取平台使用费"开关关闭时，委托采购（代理）项目不收平台使用费（0元）；开关开启时，复用招标/非招阶梯规则 |
| R12 | 计费判断顺序 | 项目立项时，先判断项目类别（招标/非招）选择对应规则，再判断组织形式（自行采购/委托采购），若为委托采购且开关关闭则计费为0 |
| R13 | 代理服务费独立 | 代理服务费与平台使用费独立，代理服务费在标段信息中配置，不在本规则中处理 |

---

### 变更5：与其他功能的关系补充（原"五"调整为"六"）

新增 6.3 节：

### 6.3 个性化配置

- 租户个性化配置弹窗独立于"设置权限"操作
- 当前仅包含"代理项目是否收取平台使用费"开关，后续可按需扩展其他开关
- 计费时结合个性化配置开关与收费规则联合计算

---

### 变更6：待确认问题补充

新增 1 条：

| 编号 | 问题 | 状态 |
|------|------|------|
| Q3 | 个性化配置弹窗的"代理项目收取平台使用费"开关是否需要在"设置权限"中保留对应的开关（避免两处都有）？ | 待确认 |

---

## 执行步骤

1. 读取 `设计文档/平台使用费收费标准配置_20260604.md`
2. 更新功能概述流程图（第13-19行）
3. 在"三、页面设计"与"四、业务规则"之间插入新的"四、租户个性化配置"章节
4. 原"四、业务规则"章节编号及后续章节编号依次后移
5. 在业务规则表中新增 R11-R13
6. 更新最后修改时间
7. 在待确认问题中新增 Q3

## 输出文件

| 文件路径 | 操作 |
|----------|------|
| `设计文档/平台使用费收费标准配置_20260604.md` | 原地编辑更新 |
