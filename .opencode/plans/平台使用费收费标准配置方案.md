# 平台使用费收费标准配置 - 执行计划

> 修改日期：2026-06-04
> 状态：待执行

---

## 变更目标

将"代理项目是否收取平台使用费"开关从独立的"个性化配置"弹窗合并进"平台使用费配置"抽屉中，去掉独立的个性化配置入口。

---

## 变更详情

### 1. 功能概述（一）

- 去掉"个性化配置"入口说明和双入口描述
- 流程图简化为单入口：

```
租户管理 → 平台使用费（配置招标类+非招标类阶梯规则 + 代理项目收费开关）
                  │
                  │ 写入租户收费规则及配置
                  ▼
采购立项 → 标段包（读取收费规则，根据组织形式判断是否计费）
```

### 2. 数据结构（二）

- **保留** `tenant_personalized_config` 表（开关仍需要存储位置），但标注该表与平台使用费配置抽屉关联
- 不变

### 3. 页面设计（三）

- 在 3.2 平台使用费配置抽屉的 ASCII 布局图中，在招标类规则区域**之前**，增加一个顶部开关区域：

```
┌─ 平台使用费配置 ─────────────────────────────────────────────────┐
│                                                                  │
│  * 代理项目收取平台使用费   [ 关 ● / 开 ○ ]（默认关）           │
│    说明：关闭后，委托采购（代理）项目不收取平台使用费            │
│                                                                  │
│  ─────────────────────────────────────────────────────────────── │
│                                                                  │
│  ▾ 招标类收费规则                                                │
│  ...（原内容不变）                                               │
│                                                                  │
│  ▾ 非招标类收费规则                                              │
│  ...（原内容不变）                                               │
│                                                                  │
│                    [ 取消 ]  [ 保存 ]                             │
└──────────────────────────────────────────────────────────────────┘
```

### 4. 去掉"四、租户个性化配置"章节

整个第四章删除，其中示例场景（集团A 2×2矩阵）迁移到"五、业务规则"后作为补充说明段落。

### 5. 章节重编号

```
一、功能概述
二、数据结构
三、页面设计（增加顶部开关）
四、业务规则（原五，含代理项目计费规则 + 集团A示例）
五、与其他功能的关系（原六）
六、待确认问题（原七）
```

### 6. 业务规则（原五→新四）

- R11-R13 保留不变
- 新增集团A示例场景作为 4.2 节（原 4.3 节迁移过来）

### 7. 与其他功能的关系（原六→新五）

- **去掉** 6.3 个性化配置小节（已合并入平台使用费配置）

### 8. 待确认问题（原七→新六）

- **去掉** Q3（不再存在"两处都有开关"的问题，因为只有一处了）

---

## 执行步骤

1. 读取 `设计文档/平台使用费收费标准配置_20260604.md`
2. 更新功能概述（文字 + 流程图）
3. 更新 3.2 抽屉 ASCII 布局图（顶部增加开关）
4. 删除"四、租户个性化配置"整章
5. 将集团A示例迁移到业务规则章节，作为补充示例
6. 所有后续章节重编号（四→五→六）
7. 去掉"与其他功能的关系"中的 6.3 个性化配置小节
8. 去掉待确认问题中的 Q3
9. 更新最后修改时间

## 输出文件

| 文件路径 | 操作 |
|----------|------|
| `设计文档/平台使用费收费标准配置_20260604.md` | 原地编辑更新 |
